namespace :explico do
  task :production do
    #`git remote rm production`
    #`git remote add production git@202.2.92.198:repository.git`
    #`git push production master`
  end
end

task :test do
  puts "No tests to run."
end
